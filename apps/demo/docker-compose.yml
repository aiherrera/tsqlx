services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: tsqlx
      POSTGRES_PASSWORD: tsqlx
      POSTGRES_DB: tsqlx_demo
    ports:
      - "5433:5432"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
