import { DEFAULT_EXAMPLE_ID, getExampleContent } from "./examples.js";

/** Default editor sample — from `examples/` (paginated list + filters). */
export const SAMPLE_TSQ = getExampleContent(DEFAULT_EXAMPLE_ID);
